<Page
    x:Class="WishList.Views.InvitesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WishList.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" xmlns:vm="using:WishList.ViewModels"
    xmlns:controls="using:Template10.Controls">

    <Page.DataContext>
        <vm:InvitesPageViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <controls:PageHeader x:Name="pageHeader" RelativePanel.AlignLeftWithPanel="True"
                             RelativePanel.AlignRightWithPanel="True"
                             RelativePanel.AlignTopWithPanel="True" Text="Wishter">

            <!--  secondary commands  -->
            <controls:PageHeader.SecondaryCommands>
                <AppBarButton Click="{x:Bind ViewModel.GoBack}" Label="Go Back" />
            </controls:PageHeader.SecondaryCommands>

        </controls:PageHeader>

        <StackPanel>
            <TextBlock Text="Received Invites" FontWeight="Bold" FontSize="20px" Margin="10,10,10,10"/>
            <ListBox x:Name="ListView1" ItemsSource="{x:Bind ViewModel.WishLists}" Width="Auto" 
                     HorizontalAlignment="Stretch" Height="Auto" VerticalAlignment="Stretch" Margin="10,10,10,10"
                     SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="0" Margin="10">
                                <TextBlock Text="Wishlist" FontWeight="Bold"/>
                                <TextBlock Name="text1" Text="{Binding Title}" TextWrapping="WrapWholeWords"/>
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="1" Margin="10">
                                <TextBlock Text="From" FontWeight="Bold"/>
                                <TextBlock Name="text2" Text="{Binding CreatorName}" TextWrapping="WrapWholeWords"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="New Invite" HorizontalAlignment="Stretch" Click="{x:Bind ViewModel.GotoNewInvites}" Margin="10"/>
        </StackPanel>
    </StackPanel>
</Page>
